package com.dyq.config;

/**
 * @author 丁艳青
 * @site www.dyq.com
 * @company xxx
 * @create 2019-01-21 21:30
 */
/*
@Configuration
@EnableConfigurationProperties(value = JDBCPro.class)
public class JDBCProConfiguration {

    @Autowired
    private JDBCPro jdbcPro;

    @Bean
    public DruidDataSource druidDataSource(){
        DruidDataSource druidDataSource = new DruidDataSource();
        druidDataSource.setDriverClassName(jdbcPro.getDriverClassName());
        druidDataSource.setUrl(jdbcPro.getUrl());
        druidDataSource.setUsername(jdbcPro.getUsername());
        druidDataSource.setPassword(jdbcPro.getPassword());
        return druidDataSource;
    }
}
*/
